# import the python datetime module to help us create a timestamp
from datetime import date

class Llama:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'petting area'
        self.walking = True

class Goat:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'petting area'
        self.walking = True

class Donkey:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'petting area'
        self.walking = True


class Camel:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'petting area'
        self.walking = True

class Buffalo:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'petting area'
        self.walking = True

class Cobra:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.slithering = True

class Gecko:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.walking = True

class Bearded_dragon:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.walking = True

class Iguana:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.walking = True

class King_snake:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.slithering = True

class Komodo_dragon:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'glass tank'
        self.walking = True

class Crocodile:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'pond'
        self.walking = True
        self.swimming = True

class Catfish:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'pond'
        self.swimming = True

class Turtle:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'pond'
        self.walking = True
        self.swimming = True

class Mallard:

    def __init__(self, name, species):
        self.date_added = date.today()
        self.home = 'pond'
        self.flying = True
        self.swimming = True


steve = Crocodile('steve', 'Crocodylus acutus' )